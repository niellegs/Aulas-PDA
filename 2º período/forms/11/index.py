for c in range(0,3):
    num = int(input("Insira um número: "))
    maiorNum = num
    if maiorNum < num:
        maiorNum = num
    else:
        maiorNum = maiorNum
print(f"o maior número digitado foi {maiorNum}")
